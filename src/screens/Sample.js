import React from 'react';
import {View, Text} from 'react-native';
import theme from 'theme';

export default () => (
  <View style={theme.container}>
    <Text style={theme.title}>{'Hello from Simple Starter :)'}</Text>
  </View>
);
